# escape=`
# Keep this dockerfile even if it does nothing useful. It serves to build Headless pull requests images of all Platform containers to easily test Headless pull requests using the democli.
ARG BASE_IMAGE

FROM $BASE_IMAGE as production
